using System;
using System.Collections.Generic;
using System.Text;

namespace DO
{
    public enum DroneStatuses { available, maintenance, delivery }

    public enum Prioritie { regular, fast, emergency }

    public enum WeightCategories { light, middle, heavey }
}
